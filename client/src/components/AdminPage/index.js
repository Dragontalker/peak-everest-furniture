import { useEffect } from "react";

function AdminPage() {

  useEffect(() => {
    //redirect if userID is not admin user
  })

  return(
    <div>
      Admin Page
    </div>
  )
}

export default AdminPage;